var Widget = (function() {
    'use strict';

    function Widget(settings) {
        if (!(this instanceof Widget)) {
            return new Widget(settings);
        }

    }
    return Widget;
})();
